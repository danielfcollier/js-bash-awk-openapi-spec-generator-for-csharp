type: object
properties:
  paymentKey:
    type: string
    description: The unique transaction ID generated by the payment gateway. To request the transaction status, you must either include the ID from the client platform or the ID from the payment gateway. If you provide both and they correspond to different transactions, an error will result.
    nullable: true
